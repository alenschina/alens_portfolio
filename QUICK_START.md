# 快速启动指南

## 🚀 启动项目

### 1. 安装依赖
```bash
npm install
```

### 2. 启动开发服务器
```bash
npm run dev
```

### 3. 访问应用
- **前台首页**: http://localhost:3000
- **后台管理**: http://localhost:3000/admin
- **管理员登录**: http://localhost:3000/admin/login

## 🔐 默认管理员账户

```
邮箱: admin@alens.com
密码: admin123
```

## 📋 功能测试清单

### ✅ 可直接测试的功能

1. **登录系统**
   - [ ] 访问 `/admin/login`
   - [ ] 使用管理员账户登录
   - [ ] 验证登录成功后跳转到仪表盘

2. **仪表盘**
   - [ ] 查看统计数据显示
   - [ ] 验证分类、图片、导航数量

3. **导航管理**
   - [ ] 查看导航列表
   - [ ] 验证多级菜单结构

4. **分类管理**
   - [ ] 查看所有分类
   - [ ] 验证每个分类的图片数量

5. **图片管理**
   - [ ] 查看所有图片网格
   - [ ] 验证图片预览
   - [ ] 查看轮播图标记

### ⏳ 待开发功能（需要继续开发）

- [ ] 创建/编辑导航项
- [ ] 创建/编辑分类
- [ ] 上传新图片
- [ ] 编辑图片元数据
- [ ] 设置轮播图
- [ ] 拖拽排序

## 🗄️ 数据库操作

### 查看数据库
```bash
npx prisma studio
```

### 重新运行种子脚本
```bash
node prisma/seed.js
```

### 重置数据库
```bash
rm prisma/dev.db
npx prisma migrate dev --name init
node prisma/seed.js
```

## 🐛 常见问题

### Q: 登录时提示 "Unauthorized"
A: 确保已运行种子脚本创建管理员账户

### Q: API 路由返回 500 错误
A: 检查数据库文件是否存在：`ls prisma/dev.db`

### Q: 图片无法显示
A: 图片使用 Unsplash CDN，需要网络连接

## 📁 项目结构

```
alens-portfolio/
├── src/
│   ├── app/
│   │   ├── admin/          # 后台管理
│   │   ├── api/            # API 路由
│   │   └── page.tsx        # 前台首页
│   ├── components/
│   │   ├── ui/             # UI 组件
│   │   └── admin/          # 管理组件
│   └── lib/                # 工具库
├── prisma/
│   ├── schema.prisma       # 数据模型
│   ├── seed.js             # 种子数据
│   └── migrations/         # 迁移文件
└── public/
    └── uploads/            # 上传目录
```

## 🔗 API 端点

### 认证
- POST `/api/auth/signin` - 登录
- POST `/api/auth/signout` - 登出

### 导航
- GET `/api/navigation` - 获取导航列表
- POST `/api/navigation` - 创建导航项

### 分类
- GET `/api/categories` - 获取分类列表
- POST `/api/categories` - 创建分类
- GET `/api/categories/[slug]/images` - 获取分类图片

### 图片
- GET `/api/images` - 获取图片列表
- POST `/api/images` - 创建图片
- POST `/api/upload` - 上传图片

## 📖 下一步开发

查看 `PROGRESS_REPORT.md` 了解详细进度和待完成任务。

---

*快速启动指南 v1.0*
